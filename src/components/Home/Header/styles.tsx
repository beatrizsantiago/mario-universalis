import styled from 'styled-components';

const Content = styled.div`
  width: 100%;
  height: 240px;
  background: linear-gradient(180deg, rgba(0,0,0,0) 0%, rgba(0,212,255,1) 80%);
`;

export default Content;
