import React from 'react';
import { Container, Input, SearchIcon } from './styles';

const SearchInput = () => (
  <Container>
    <Input />
    <SearchIcon />
  </Container>
);

export default SearchInput;
